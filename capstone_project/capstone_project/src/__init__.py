"""Triage Toolkit package.

A small end-to-end security triage CLI combining:
- network log analysis (csv + Counter)
- file integrity hashing (hashlib) with SQLite-backed history
- local port checks (socket)
- external API lookups (requests)
"""
